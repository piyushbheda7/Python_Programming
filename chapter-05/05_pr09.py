# Can you change the values inside a list which is contained in set S

s = {8 , 7 , 12 , 'harry' , [1,2]}

# this will throws an error because list can't be added into set 
# list is mutable but set is immutable